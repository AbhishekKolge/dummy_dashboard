import { Archive, ArchiveX, File, Inbox, Send, Trash2 } from 'lucide-react';
import { LucideIcon } from 'lucide-react';

export const DATA: {
  title: string;
  label?: string;
  icon: LucideIcon;
}[] = [];
